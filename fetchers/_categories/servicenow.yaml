description: >
  ServiceNow platform evidence. Customer service cases and ITSM change records
  pulled from scoped-application REST APIs using a shared service-account
  credential over basic auth. Each fetcher takes the full endpoint URL, because
  the scoped-app path differs per instance.
