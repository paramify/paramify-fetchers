"""Uploaders — the separate stage that pushes collected evidence to Paramify.

Packaged so `paramify upload` works from an installed distribution; in a dev
checkout the api loads uploader.py by path from this same tree.
"""
